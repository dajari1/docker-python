pipeline{
    agent any
    stages {
        stage("github checkout"){
            steps{
                echo "github checkout code"
            }
        }
        stage("build docker image"){
            steps{
                echo "build docker image"
            }
        }
        stage("push image to dockerhub"){
            steps{
                echo "push image to dockerhub"
            }
        }
    }


}